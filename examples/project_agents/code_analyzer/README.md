# Enhanced Code Analyzer

Analyzes trading scanner code with static analysis, pattern detection, and market data validation. Based on RENATA CodeAnalyzerAgent + TradingAgent patterns.

## Installation

```bash
pip install -r requirements.txt
```

## Configuration

Set up environment variables:

```bash
# For Neo4j (if using)
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=password

# For OpenAI embeddings (if using)
OPENAI_API_KEY=sk-...

# For LLM (if using)
ANTHROPIC_API_KEY=sk-...
# or
OPENAI_API_KEY=sk-...
```

## Usage

```python
from enhanced__code__analyzer_agent import EnhancedCodeAnalyzerAgent

async def main():
    # Create agent
    agent = EnhancedCodeAnalyzerAgent()

    # Connect to vector database
    await agent.connect_vector_db()

    # Execute task
    result = await agent.execute(
        task="Your task here",
        use_knowledge=True  # Enable RAG
    )

    print(result)

    # Close connection
    await agent.close_vector_db()

if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

## Tools

This agent has 8 tools:

- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}
- {tool['name']}: {tool.get('description', 'No description')}

## RAG Configuration

- **Enabled**: True
- **Vector DB**: neo4j
- **Collection**: code_analysis_knowledge
- **Top K**: 5

## Development

This agent was auto-generated by CE Hub v2 Agent Builder.

**Generated**: 2026-01-05 11:44:07
